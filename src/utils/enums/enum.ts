export enum RedisEnum {
  jit = 'jit',
  profile = 'profile',
  follower = 'follower',
  following = 'following',
  comment = 'comment',
  message = 'message',
  conversation = 'conversation',
  member = 'member',
}

export enum MinioEnum {
  avatars = 'avatars',
  backgrounds = 'backgrounds',
  image = 'image',
  video = 'video',
}
