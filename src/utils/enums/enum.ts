export enum RedisEnum {
  jit = 'jit',
  profile = 'profile',
  follower = 'follower',
  following = 'following',
  comment = 'comment',
}

export enum MinioEnum {
  avatars = 'avatars',
  backgrounds = 'backgrounds',
}
